# Godot 4+ specific ignores
.godot/
.nomedia

# Diagrammi in docs/: sono immagini di documentazione, non asset di gioco.
# Godot genera .import file quando ri-importa il progetto — ignoriamoli per
# evitare rumore nel git status.
v1/docs/diagrams/*.import

# Godot-specific ignores
.import/
export.cfg
export_credentials.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# CI/CD build artifacts
export/

# Old project scaffold (non necessario)
projectwork/

# Reference assets (locali, non versionati)
REFERENCES-IMPORTANT/

# Claude Code workspace (locale)
.claude/

# Technical guide (developer-only, non versionare)
TECHNICAL_GUIDE.md

# SQLite databases (user data, not versioned)
*.db
*.db-wal
*.db-shm

# Secrets and environment
.env
*.cfg.local

# Playwright MCP (local testing tool)
.playwright-mcp/

# Asset collection guide (developer-only)
ASSET_COLLECTION_GUIDE.md

# VS Code workspace (local)
*.code-workspace

# macOS resource fork archives
__MACOSX/

# OS-generated files
.DS_Store
Thumbs.db
desktop.ini
v1/SUPERVISOR_WORKPLAN.md

# PDF export artifacts (generated, not versioned)
v1/export_pdf/
Mini-Cozy-Room-Presentazione-Progetto.pdf

# Netlify local state (auto-generated, not versioned)
.netlify/

# Old integration materials (already merged into v1/)
files/

# Loose screenshots/images at repo root
/1234.png
/567.png
/890.png
/DioB.png
/DioC.png
/cozyico.png
